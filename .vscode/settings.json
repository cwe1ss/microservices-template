{
    "files.associations": {
        // Bicep can handle comments when loading ./infrastructure/config.json
        // This allows us to add comments directly into the file without VSCode complaining about it.
        "config.json": "jsonc"
    }
}
