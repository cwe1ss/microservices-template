{
    "files.associations": {
        // Bicep can handle comments when loading JSON files.
        // This allows us to add comments directly into the files without VSCode complaining about it.
        "config.json": "jsonc",
        "names.json": "jsonc"
    }
}
